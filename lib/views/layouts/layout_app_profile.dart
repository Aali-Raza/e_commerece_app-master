import 'package:flutter/material.dart';

class LayoutAppProfile extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("profile"),),
    );
  }
}
