import 'package:flutter/material.dart';

class LayoutUserChat extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("chats"),),
    );
  }
}
