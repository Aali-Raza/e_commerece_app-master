import 'package:flutter/material.dart';

class LayoutAppHome extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("home"),
      ),
    );
  }
}
